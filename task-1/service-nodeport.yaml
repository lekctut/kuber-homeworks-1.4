apiVersion: v1
kind: Service
metadata:
  name: svc-multi-deployment-nodeport
spec:
  type: NodePort
  selector:
    app: multi-deployment
  ports:
    - name: nginx
      protocol: TCP
      port: 9001
      targetPort: 80
      nodePort: 30000